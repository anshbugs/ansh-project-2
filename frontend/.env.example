# Optional: backend API base URL (no trailing slash).
# Set this on Vercel when the chat API is deployed elsewhere (e.g. Render, Railway).
# Example: https://your-backend.onrender.com
# Leave unset for local dev with proxy or same-origin API.
# VITE_API_URL=
